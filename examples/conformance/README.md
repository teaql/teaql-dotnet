# .NET runtime conformance example

This retained SQLite example is generated from `model.xml` and verifies explicit `EnsureSchemaAsync`, Create, Update, Delete, typed Q/SmartList, E loaded/null/not-loaded semantics, and Checker rejection before SQL.

```bash
dotnet build
dotnet run
```
